{
  "name": "Webhook Dispatch - Local",
  "values": [
    {
      "key": "base_url",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "Base URL of the running application"
    },
    {
      "key": "internal_token",
      "value": "change-me-secret",
      "type": "secret",
      "enabled": true,
      "description": "Shared secret for POST /internal/events — must match INTERNAL_TOKEN in .env"
    }
  ]
}
